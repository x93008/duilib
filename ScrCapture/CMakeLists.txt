#CMake file for ScrCapture Demo
#Author: Generated by AI
#Date: 2025/09/30

set(source_files 
    App.cpp
    Canvas.cpp
    CanvasContainer.cpp
    ScrCaptureWnd.cpp
    Util.cpp
    StdAfx.cpp
)

include_directories(${CMAKE_CURRENT_SOURCE_DIR})

set(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)
add_executable(ScrCaptureDemo ${source_files})
add_dependencies(ScrCaptureDemo duilib)
target_link_libraries(ScrCaptureDemo duilib)
set_target_properties(ScrCaptureDemo PROPERTIES LINK_FLAGS "/SUBSYSTEM:WINDOWS")

# Set UTF-8 encoding and runtime library
if(WIN32 AND MSVC)
    target_compile_options(ScrCaptureDemo PRIVATE /utf-8)
    target_compile_options(ScrCaptureDemo PRIVATE /MT$<$<CONFIG:Debug>:d>)
endif()

install(
  TARGETS ScrCaptureDemo
  RUNTIME DESTINATION bin
  LIBRARY DESTINATION lib
  ARCHIVE DESTINATION lib
)
